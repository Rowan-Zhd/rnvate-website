<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Dark background -->
  <rect width="64" height="64" fill="#060608"/>

  <!-- White left accent bar (paired with the flipped wordmark direction) -->
  <rect x="0" y="0" width="5" height="64" fill="#f7f7f9"/>

  <!-- "RN" in mint, matching the flipped nav logo treatment -->
  <text
    x="10" y="44"
    font-family="'Arial Black', 'Arial Bold', Arial, sans-serif"
    font-weight="900"
    font-size="30"
    letter-spacing="-1"
    fill="#04E98A"
  >RN</text>

  <!-- White underline accent -->
  <rect x="10" y="50" width="46" height="3.5" fill="#f7f7f9"/>
</svg>
